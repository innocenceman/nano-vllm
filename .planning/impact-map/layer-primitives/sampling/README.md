# `layer-primitives/sampling` Group

Generated: 2026-05-12

This is an intermediate architecture grouping. Final leaf packets are below; this grouping file intentionally avoids pretending the group itself is a testable leaf.

## Leaves

- [`sampling-kernel`](sampling-kernel/README.md) — `exceptioned-deep-partial` — Convert logits and per-sequence temperatures into sampled token IDs using exponential-race sampling.
